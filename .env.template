# N100 Financial Intelligence Platform - Environment Configuration
# Copy this file to .env and adjust values as needed.
# .env itself should NEVER be committed to git (it's in .gitignore) --
# .env.template IS committed, so teammates know what variables exist.
DB_PATH=data/nifty100.db
PORT=8000
LOG_LEVEL=INFO
SIMULATED_DATA_FLAG=true
